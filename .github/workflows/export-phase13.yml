name: Export current main source
on:
  push:
    branches: [ codex/export-phase13 ]
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 1
      - name: Pack source
        run: tar --exclude=.git -czf /tmp/go-smpp-main-source.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: go-smpp-main-source
          path: /tmp/go-smpp-main-source.tar.gz
          retention-days: 1
